# $D(SP)^2$ - Comandos de Build e Regras

## Regras de Arquitetura e Hardware (OBRIGATÓRIO LER)
Para qualquer alteração no código C++ (`/core`, `/nodes_cpp`), regras de performance em tempo real **você deve ler e seguir o arquivo `AGENTS.md`** localizado na raiz do projeto.